**Models are named according to their architecture.**
**Each Model.ipynb file creates two pdf's (training loss & FID score), Model_FID.npy.**
**Each model uses FID.ipynb for scoring performance.**
**compare_performance.ipynb creates a image of comparison between models.**
